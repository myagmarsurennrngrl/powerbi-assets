# expo-router pulls a web-only react-dom that declares a stricter react peer range
# than the react version Expo SDK 57 pins. Expo's own installer overrides this the
# same way. Without it, `npm install` fails with ERESOLVE on a clean checkout.
legacy-peer-deps=true
